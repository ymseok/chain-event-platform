# Node environment
NODE_ENV=development

# Server
PORT=3003

# Admin API
ADMIN_API_URL=http://localhost:3001/api/v1
INTERNAL_API_KEY=your-internal-api-key

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/chain_event_platform

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Webhook settings
WEBHOOK_TIMEOUT_MS=10000
CONCURRENCY_PER_APP=1

# Stream settings (defaults shown — uncomment to override)
# STREAM_BLOCK_TIMEOUT_MS=5000
# STREAM_MIN_IDLE_MS=120000
# STREAM_RECOVERY_INTERVAL_MS=10000
# STREAM_MAX_RECOVERY_PER_CYCLE=100
# STREAM_MAX_DELIVERY_COUNT=10
